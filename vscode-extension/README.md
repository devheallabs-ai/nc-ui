# NC UI VS Code Extension

This extension adds:
- `.ncui` language registration
- syntax highlighting
- snippets
- a lightweight NC UI language server for diagnostics and completions

It is intentionally plain-English focused and uses the repo's built-in compiler.
